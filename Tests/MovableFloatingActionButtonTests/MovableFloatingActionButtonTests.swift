import XCTest
@testable import MovableFloatingActionButton

final class MovableFloatingActionButtonTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MovableFloatingActionButton().text, "Hello, World!")
    }
}
